import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles(theme => ({
  formContainer: {
    marginBottom: 24,
    maxWidth: 360,
    padding: 16
  },
  formItem: {
    margin: '12px 0px'
  }
}))

export default useStyles
